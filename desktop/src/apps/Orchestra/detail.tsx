import React from 'react';
import { Box } from '@mui/material';

interface DagDetailProps {
  dag: any;
  open: boolean;
  onClose: () => void;
}

const DagDetail: React.FC<DagDetailProps> = () => {
  return <Box />;
};

export default DagDetail;